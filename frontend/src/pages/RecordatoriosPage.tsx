import { Bell } from "lucide-react";
import PlaceholderPage from "./PlaceholderPage";

export default function RecordatoriosPage() {
  return (
    <PlaceholderPage
      icon={Bell}
      title="Recordatorios"
      description="Citas médicas, vacunas y recordatorios pendientes."
    />
  );
}
